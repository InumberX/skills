---
name: <kebab-case-verb-phrase>
description: <What this skill does + when to use it, in English. Include concrete request patterns, e.g. "Use when the user asks to ...">
---

# <skill-name>

<このスキルが何をするかの一文。対象範囲(どのコード・どの成果物か)も書く。>

## いつ発動するか

次のいずれかに該当する場合に使う:

- 「<ユーザーの依頼パターン 1>」と依頼された
- 「<ユーザーの依頼パターン 2>」と依頼された

<機械検出できる項目をツールに委譲する場合はここに明記する。例: 「フォーマット・未使用変数などは `npm run lint` に任せ、本スキルは人間の判断が必要なルールにフォーカスする。」>

## 手順

### 1. <最初のステップ>

<手順の説明。分割ファイルを読むタイミングはここで指示する。>

### 2. <観点の選択(分割ファイルがある場合)>

| 依頼パターン | 参照ファイル |
|---|---|
| 「<パターン>」 | `rules/<category>/<file>.md` |

### 3. <実行>

<実行時の注意。ルールは推測で適用せず、ファイルから引用して根拠を示す。>

## 出力フォーマット

<成果物の形式。テンプレートがあれば示す。>

## 拡張ガイド(将来の作業者向け)

<観点・ファイルの追加手順。「既存ファイルへの追記で済まないか先に検討する」を含める。>
