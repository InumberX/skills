---
name: review-pr
description: "Review a pull request against a project's style, naming, and structural conventions, and against framework- and platform-level security checks scoped to the changed files. Use when the user asks for a PR review, code review, or review of pending changes — including narrowed-scope requests like 'review the styling', 'check the naming', or 'check these changes for security issues'. Provides the cross-project review procedure and output format; style rules live in each project's own .claude/skills/review-pr/rules/, while the security rules ship with this skill."
---

# review-pr

複数プロジェクトで共通のコードレビュー手順・出力フォーマットを提供するスキルの**骨格**。手順・重要度分類・拡張ガイドはどのプロジェクトでも同じなので中央に置き、**具体的なレビュー観点（命名・CSS・構造などの規約本文）は各プロジェクトの `.claude/skills/review-pr/rules/` に置く**。

例外はセキュリティ観点で、これだけは `rules/security/` として本スキルに同梱している。参照するのがフレームワーク・プラットフォームの規定する識別子とファイル名（`ssr`・`define`・`run_worker_first` のような設定キー、`next.config.*`・`proxy.ts` / `middleware.ts` のようなファイル名など、どのリポジトリでも同じ名前になるもの）だけで、**そのリポジトリ固有のパス・識別子を引用しない**ため中央で保守できる。

機械的に検出できる項目（フォーマット、未使用変数、`any` 禁止、import 順序、フック依存配列など）は各プロジェクトの `lint`・`format`・`typecheck` に任せ、本スキルは**人間の判断が必要なルール**にフォーカスする。

## いつ発動するか

次のいずれかに該当する場合に使う:

- 「PR レビューして」「コードレビューして」「変更をレビューして」と依頼された
- 「スタイルだけ見て」「命名だけチェックして」など特定観点のレビューを依頼された
- 「セキュリティチェックして」「この変更は安全か」など、変更差分に対する安全性の確認を依頼された
- PR の差分（`git diff`、GitHub の PR、ステージング済みの変更）に対する品質評価を求められた

## このスキルの構成（骨格と本文の分離）

| 役割 | 置き場所 |
|---|---|
| レビュー手順・出力フォーマット・拡張ガイド（本ファイル） | 本リポジトリ（横断・不変の骨格） |
| 実際のレビュー観点（命名・CSS・構造などの規約本文と例） | 各プロジェクトの `.claude/skills/review-pr/rules/`・`examples/` |
| セキュリティ観点（フレームワーク・プラットフォーム由来） | 本リポジトリの `rules/security/`（本スキルに同梱） |

各プロジェクトはスタイル基盤（例: CSS Modules と Vanilla Extract）やディレクトリ規約が異なるため、`rules/` の中身は完全に別物になる。**本ファイルをプロジェクトへ取り込む際は、そのプロジェクトの `rules/` を必ず用意する**（同梱の `rules/security/` を除き、骨格だけでは観点が空になる）。

## レビュー手順

### 1. 差分の取得

優先順位:

1. ユーザーが PR 番号や URL を指定 → `gh pr view <番号>` / `gh pr diff <番号>` で取得（環境で提供される GitHub MCP の PR 取得ツールが使えるならそれでもよい。ツール名は MCP 実装により異なる）
2. ブランチがデフォルトブランチ（`develop` / `main` 等）から派生している → `git diff <base>...HEAD --stat` と `git diff <base>...HEAD` で差分把握
3. 作業ツリーに未コミット変更がある → `git status` と `git diff`

### 2. 観点の選択

依頼内容から、そのプロジェクトの `rules/` 配下の参照ファイルを決定する。実際のカテゴリはプロジェクト側が定義するが、典型的な対応は次の通り:

| 依頼パターン | 参照ファイル（プロジェクトの `rules/` 配下） |
|---|---|
| 「PR レビュー」「全体レビュー」「コードレビュー」 | `rules/**/*.md` 全て + 本スキルの `rules/security/`（下記の検出条件に一致するもの） |
| 「スタイル」「命名」「CSS」「構造」関連 | `rules/style/*.md` |
| 「命名だけ」 | `rules/style/naming.md` |
| 「CSS だけ」「クラス名だけ」 | `rules/style/css.md` |
| 「ディレクトリ構造」「ファイル配置」 | `rules/style/structure.md` |
| 「セキュリティ」「安全性」「脆弱性」 | 本スキルの `rules/security/`（下記の検出条件に一致するもの）のみ |

プロジェクトが `rules/a11y/`・`rules/performance/`・`rules/i18n/` 等を追加していれば、同様に依頼内容へマッピングする。

セキュリティ観点だけは依頼の言葉ではなく、**リポジトリ側の構成**で読むファイルを決める。該当しないファイルは読まない。

| 検出条件 | 参照ファイル |
|---|---|
| 常に | `rules/security/boundary.md` |
| `package.json` の依存に `wrangler` がある | `rules/security/cloudflare-workers.md` |
| `package.json` の依存に `next` がある（※） | `rules/security/nextjs.md` |
| `package.json` の依存に `@react-router/dev` がある | `rules/security/react-router.md` |

※ App Router のみが対象。`app/` ディレクトリが無く Pages Router だけで構成されているリポジトリでは読み飛ばす（依存だけでは両者を区別できないため、ディレクトリで確認する）。

**設定ファイル（`next.config.*` など）の有無で判定しない。** これらはいずれも省略可能で、無いまま運用しているリポジトリではルールが読まれなくなる。判定は依存パッケージで行い、**そのルールが対象とする使い方に固有のパッケージを選ぶ**（`react-router` は library mode でも入るため、framework mode の指標である `@react-router/dev` を見る）。

モノレポでルートの `package.json` に依存が無い場合は、差分に現れたファイルを含むパッケージの `package.json` を見る。

### 3. ルールの読み込みとレビュー

選択したルールファイルを実際に読み、差分の各ファイルに対してルール違反を検出する。**ルールを推測で適用せず、必ずファイルから引用して根拠を示す。** 判断に迷ったらプロジェクトの `examples/` と既存コードを参照する。

#### セキュリティ観点の扱い

- **範囲は差分に現れたファイルを「起点」にする。** PR 全体の棚卸しはしない。ただし変更箇所と、それが依存する不変条件の定義箇所が別ファイルに分かれていることがある（設定を追加した側と、その設定を検証・列挙している側など）。起点から辿れる定義箇所までは確認し、無関係なファイルは走査しない
- **汎用の脆弱性検出は Claude Code 組み込みの `/security-review` に委譲する。** SQL/コマンドインジェクション、XSS、ハードコードされた秘密といった一般的なパターンは組み込みが担当する。本スキルの `rules/security/` は、組み込みが明示的に除外する項目（ハードニング不足・設定由来の無効化・タイミング攻撃・環境変数由来の問題）と、フレームワーク固有の境界誤認だけを扱う。両方書くと同じ指摘が二重に出る
- 重要度は規約違反の軸（Must / Should / Nit）ではなくリスクの軸で付ける。出力は独立セクションに分ける

### 4. 出力フォーマット

指摘は重要度別に整理し、ファイルパスと行番号を必ず含める。各指摘には**参照したルールファイルとセクションを引用**する。

セキュリティ指摘は「規約違反かどうか」ではなくリスクの大きさで並べるため、独立セクションに分けて High / Medium / Low を使う。該当が無ければセクションごと省く。

````markdown
## レビュー結果

### 🔒 セキュリティ

- **`<file>:<line>` — High**
  何がどう悪用され得るかを一文で述べ、根拠として参照した `rules/security/...` のファイル・セクションを引用する。修正案を添える。

### 🔴 Must（規約違反・修正必須）

- **`<file>:<line>`**
  何がどのルールに違反しているかを一文で述べ、根拠として参照した `rules/...` のファイル・セクションを引用する。必要なら Bad/Good の最小コード例を添える。

### 🟡 Should（推奨される改善）

- **`<file>:<line>`**
  規約違反ではないが、既存コードと一貫させると良い点。

### 🟢 Nit（任意の好み）

- ...

### ✅ 良かった点

- 規約に正しく従えている点を挙げる。
````

## 出力上の注意

- 機械的検出可能な指摘（lint で出るもの）は基本的に書かない。書く場合は「lint で検出可能」と添える
- 「規約違反」と「好みの差」を明確に分ける（Must / Should / Nit）
- 参照したルールファイルとセクションを必ず引用する
- ファイルが大量にある PR では、まず観点別の集計（「naming 違反 N 件、css 違反 M 件」）を提示してから個別指摘に入る
- そのプロジェクトのスタイル基盤に即したコメントにする（他プロジェクトの規約を混同しない）
- セキュリティ指摘は可能なかぎり「悪用され得る筋道」まで書く。単一の経路として書けないもの（ハードニング不足など、本スキルが担当すると決めた領域に多い）は Low に置き、**何が守られなくなるか**を代わりに添える。規約違反の軸（Must / Should / Nit）へ移し替えない

## 拡張ガイド（将来の作業者向け）

- **観点を増やすとき**（例: アクセシビリティ）は、対象プロジェクトの `.claude/skills/review-pr/` 側で `rules/<category>/`・`examples/<category>/` を追加し、そのプロジェクトの SKILL.md の「観点の選択」テーブルに行を足す。**本ファイル（中央の骨格）にプロジェクト固有の観点や実コードのパスを書かない。**
- 例外は `rules/security/` で、ここだけは本リポジトリに観点本文を置く。線引きは「何を引用するか」で判断する。**フレームワーク・プラットフォームが規定する識別子とファイル名までなら中央**（`ssr`・`run_worker_first` のような設定キー、`next.config.*`・`proxy.ts` / `middleware.ts`・`route.ts` のような公式に名前が決まっているファイル。どのリポジトリでも同じ名前になり、そのリポジトリの都合では変わらない。世代によって改称されたものは両方を併記する）。**そのリポジトリにしか存在しないパス・識別子（コンポーネント名、独自に切ったサーバーモジュールのパスなど）を引用するならプロジェクト側。** プロジェクト固有の不変条件（このリポジトリのこのファイルがこの前提を守っている、という類）は中央に書かない
- **`rules/security/` にフレームワーク別ファイルを足すとき**（`nextjs.md`・`react-router.md` など）は、冒頭に対象バージョンを明記し、範囲外は「未検証」と書く。フレームワークの世代交代で内容が陳腐化しても、配布先からは古さが見えないため。同じフレームワークでも設定（SSR の有無など）で結論が反転するものは、フレームワーク名ではなく設定値で分岐させて書く
- **`rules/security/` にファイルを足したら**「観点の選択」の検出条件テーブルにも行を足す。検出条件が書かれていないファイルは読まれない。検出条件は**依存パッケージ**で書く（設定ファイルは省略可能なことが多く、取りこぼす）。ただしパッケージ名は**そのルールが対象とする使い方に固有のもの**を選ぶ。広く使われるパッケージ名を条件にすると、対象外の構成まで拾って全項目が空振りする（例: `react-router` は library mode でも入るので framework mode の判定に使えない）。依存だけで区別できない場合は、ディレクトリの有無など補助条件を注記する
- **`boundary.md` は常に読まれる**ため、フレームワーク別ファイルと必ず同時に読まれる。同じ論点を両方に詳しく書かず、`boundary.md` に原則、フレームワーク別に具体的な機構と見つけ方を置く（`writing.md`「ファイルを増やすのは、読み込み単位を分けたいときだけ」）
- **手順・出力フォーマット・重要度分類そのものを変えるとき**は本ファイルを更新する。骨格の変更は全プロジェクトへ影響するため、取り込み済みプロジェクトとの整合を確認する
- 同じ観点が複数プロジェクトの `rules/` で実質同一になったら、その観点の共通部分も本リポジトリへ昇格できないか検討する（判断基準は `../create-skill/rules/placement.md`）
